package models

import scala.slick.driver.PostgresDriver.simple._
import Database.threadLocalSession

//case class User(id: Option[Long], name: String, status: UserStatus, email: String, password: String) extends Person(id, name, status, Some(email))

